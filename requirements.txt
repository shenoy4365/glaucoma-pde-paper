# Python Dependencies for PDE-Based Glaucoma Modeling

# Core numerical computing
numpy>=1.24.0
scipy>=1.10.0

# Visualization
matplotlib>=3.7.0
seaborn>=0.12.0

# Data handling
pandas>=2.0.0

# Jupyter for interactive exploration
jupyter>=1.0.0
ipykernel>=6.20.0

# Testing
pytest>=7.2.0
pytest-cov>=4.0.0

# Code quality
black>=23.0.0
flake8>=6.0.0

# Optional: FEniCS (install via conda or Docker)
# fenics-dolfin>=2019.1.0  # May not work on M1, use conda instead

# For mesh generation and visualization
meshio>=5.3.0
pygmsh>=7.1.0  # If creating custom meshes

# Progress bars for long computations
tqdm>=4.65.0

# Optional: Advanced visualization
# mayavi>=4.8.0  # 3D visualization (install separately if needed)

# LaTeX integration
# latexcodec>=2.0.0  # For LaTeX in matplotlib

# GPU acceleration (optional, for future work)
# cupy>=12.0.0  # CUDA acceleration for NumPy operations

# Profiling and performance
line_profiler>=4.0.0
memory_profiler>=0.61.0

# Documentation
sphinx>=6.0.0  # If you decide to create API docs

# ============================================
# Installation Notes:
# ============================================
#
# 1. Basic installation (without FEniCS):
#    pip install -r requirements.txt
#
# 2. FEniCS installation:
#    conda create -n fenics -c conda-forge fenics
#    conda activate fenics
#    pip install -r requirements.txt
#
# 3. Docker (alternative):
#    docker pull dolfinx/dolfinx:stable
#
# 4. Verify:
#    python -c "import numpy, scipy, matplotlib; print('OK')"
